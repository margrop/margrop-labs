# AI 面试工作台 P5-014 技术安全报告

更新时间：2026-07-29

## 结论

P5-014 的合成与对抗基准通过。当前结果只证明本地输入、确定性解析、结构化投影和工作台边界的技术安全行为；**不证明真实招聘质量、公平性结果或生产可用性**。

机器报告由 `buildInterviewP5014Report()` 生成，版本为 `p5-014`，范围为 `synthetic-only`。

## 覆盖矩阵

共 8 个可重复案例，全部通过：

- 中英混合文本；
- 含日文叙述的结构化文本；
- 缺少成果、规模和可确认证据的输入；
- 毕业年份、学校等代理变量；
- 英文和中文提示注入；
- HTML/SVG 主动标记；
- 超过 32 KiB 的输入；
- NUL 控制字符。

代理变量与显式受保护属性在进入匹配前失败关闭。提示注入和主动标记只保留安全解析提醒，不进入结构化结果。

## 安全回归

- AI 请求边界只接收版本化 allowlist 结构化投影，不接收原始简历/JD；
- 原文不进入 URL、Analytics、错误/日志文本或安全 Markdown 导出；
- 接受的本地工作流始终要求人工确认；
- 所有结论保持 `draft`，自动录用和自动淘汰均关闭；
- `unknown`、缺失证据和反证继续保留为可见的人工核验状态；
- AI 或网络失败不阻断确定性本地流程。

## 招聘质量证据边界

本报告没有以下证据：

- 真实候选人数据；
- 真实岗位的招聘结果或运营漏斗；
- 受保护群体的真实结果差异研究；
- 面试官一致性、候选人体验或误伤率观察。

因此真实招聘用途仍为 **NO-GO**，本报告不能替代 P5-015 的 Beta 出口评审。

## 验证入口

- `apps/web/src/lib/interview-adversarial.ts`
- `apps/web/src/lib/interview-adversarial.test.ts`
- `apps/web/src/lib/interview-import.test.ts`
- `e2e/interview-workbench-reliability.spec.ts`
