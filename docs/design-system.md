# 设计基线

视觉方向：工程实验台，而不是聊天软件。

- 高信息密度但保持清晰层级；
- 证据、规则、AI 解释和警告使用不同且带文字的状态；
- 主要交互在 390px 手机宽度可完成；
- 不用颜色作为唯一状态表达；
- 动画尊重 `prefers-reduced-motion`；
- 长文本支持折叠、复制和安全下载；
- 错误说明“发生了什么、哪些结果仍可信、下一步怎么做”；
- 不使用持续闪烁、伪终端噪声或无意义的 AI 打字动画。
