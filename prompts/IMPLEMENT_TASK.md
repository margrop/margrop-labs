# Implement One Margrop Labs Task

阅读根目录 `AGENTS.md`、任务卡、目标 Lab 的 `README.md` 和 `lab.json`，以及任务引用的规范与 ADR。

先输出简短计划：边界、文件、隐私风险、AI 成本风险和验证方式。只完成一个 TODO，不顺手实现相邻功能。

把解析、验证、计算和脱敏做成确定性、可离线测试的核心。只有确有必要时才调用 AI，并使用版本化结构化输出、最小化输入、严格上限和降级路径。

不得连接真实基础设施，不得提交或打印密钥，不得把用户输入放进 URL、Analytics、服务日志和测试快照。所有演示使用合成数据。

完成时报告：变更摘要、验证结果、隐私影响、AI 成本影响、已知限制和下一条最小任务。缺少会改变架构、安全边界或公共合同的信息时，停止并提出一个具体问题。
