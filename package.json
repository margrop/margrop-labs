{
  "name": "margrop-labs",
  "version": "0.0.0",
  "private": true,
  "packageManager": "npm@11.9.0",
  "engines": {
    "node": "24.14.0",
    "npm": "11.9.0"
  },
  "workspaces": [
    "apps/*",
    "packages/*"
  ],
  "scripts": {
    "format": "prettier --write apps/web/src apps/web/public apps/web/scripts apps/web/*.json apps/web/*.jsonc apps/web/*.mjs e2e packages scripts \"labs/**/*.json\" \"schemas/**/*.json\" package.json eslint.config.mjs prettier.config.mjs vitest.config.ts playwright.config.ts .github/workflows",
    "format:check": "prettier --check apps/web/src apps/web/public apps/web/scripts apps/web/*.json apps/web/*.jsonc apps/web/*.mjs e2e packages scripts \"labs/**/*.json\" \"schemas/**/*.json\" package.json eslint.config.mjs prettier.config.mjs vitest.config.ts playwright.config.ts .github/workflows",
    "lint": "eslint .",
    "typecheck": "npm run check --workspace @margrop-labs/web && npm run typecheck --workspace @margrop-labs/ai-gateway && npm run typecheck --workspace @margrop-labs/redaction",
    "test": "vitest run",
    "test:browser": "node scripts/run-browser-tests.mjs",
    "benchmark:token-forge": "vitest run apps/web/src/lib/token-forge-benchmark.test.ts",
    "build": "npm run build --workspace @margrop-labs/web",
    "verify:static": "npm run verify:static --workspace @margrop-labs/web",
    "security:repository": "node scripts/security/repository-security.mjs",
    "security:dependencies": "npm audit --audit-level=high",
    "security": "npm run security:repository && npm run security:dependencies",
    "validate": "npm run format:check && npm run security && npm run lint && npm run typecheck && npm run test && npm run build && npm run verify:static && npm run test:browser"
  },
  "devDependencies": {
    "@eslint/js": "10.0.1",
    "@playwright/test": "1.60.0",
    "@sparticuz/chromium": "148.0.0",
    "ajv": "8.20.0",
    "ajv-formats": "3.0.1",
    "eslint": "10.8.0",
    "eslint-plugin-astro": "1.6.0",
    "globals": "17.7.0",
    "prettier": "3.9.6",
    "prettier-plugin-astro": "0.14.1",
    "typescript-eslint": "8.65.0",
    "vitest": "4.1.10"
  }
}
